const pool = require('../db/database'); // Import your database configuration

createUser = async (req, res) => {
   try {
    // res.send(req.body);
   } catch (error) {
    
   }
}

module.exports = {
    createUser,
}